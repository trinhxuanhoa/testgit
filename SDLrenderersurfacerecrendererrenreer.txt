SDL_Texture *anh;
SDL_Rect a;
a.x=0;
a.y=0;
a.w-0;
a.h=0
SDL_RenderSetViewport(anh,a); // xác định vị trí vẽ ảnh và kích thước ảnh để không phải set mỗi lần
SDL_RenderCopy(renderer,a,NULL,NULL) (vẽ ảnh);

SDL_QueryTexture(a,NULL,NULL,&a.w,&a.h) // trả về kích thước thật của ảnh
Uint8 red=255,green=128, blue=255,a;
SDL_SetTextureColorMod( people, red, green, blue ); // chỉnh màu cho ảnh
SDL_SetTextureBlendMode( anh,SDL_BLENDMODE_BLEND ); // chuẩn bị hòa chộn
SDL_SetTextureAlphaMod( anh, a );  // độ tương phản khi hòa trộn a chỉnh tương phản

Cụ thể, SDL_RendererFlip a =:có ba giá trị:
SDL_FLIP_NONE: Không lật texture
SDL_FLIP_HORIZONTAL: Lật texture theo chiều ngang
SDL_FLIP_VERTICAL: Lật texture theo chiều dọc

SDL_MOUSEMOTION: Được kích hoạt khi chuột được di chuyển.
SDL_MOUSEBUTTONDOWN: Được kích hoạt khi một nút của chuột được nhấn.
SDL_MOUSEBUTTONUP: Được kích hoạt khi một nút của chuột được thả ra sau khi đã được nhấn.

 const Uint8* currentKeyStates = SDL_GetKeyboardState( NULL );
                if( currentKeyStates[ SDL_SCANCODE_UP ] )
                {
                    currentTexture = &gUpTexture;
                }
                khác nếu(currentKeyStates[ SDL_SCANCODE_DOWN] )
                {
                    currentTexture = &gDownTexture;
                }
                khác nếu(currentKeyStates[ SDL_SCANCODE_LEFT] )
                {
                    currentTexture = &gLeftTexture;
                }
                khác nếu(currentKeyStates[ SDL_SCANCODE_RIGHT] )
                {
                    currentTexture = &gRightTexture;
                }
                khác
                {
                    currentTexture = &gPressTexture;
                }